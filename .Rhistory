data(mtcars)
head(mtcars)
x = cbind(1, mtcars$wt, mtcars$hp)
y = mtcars$mpg
head(x)
solve(t(x) %*% x) %*% t(x) %*% y
beta = solve(t(x) %*% x) %*% t(x) %*% y
coef(lm(mpg ~ wt + hp, data + mtcars))
coef(lm(mpg ~ wt + hp, data = mtcars))
head(mtcars)
data(mtcars)
head(mtcars)
